<div class ="container">

  {{!-- <div class="jumbotron">
    <h1>Burger Order</h1>
    <p>Burger Orders, Burger Pickup.</p>
    <br>
    <h2>Select a burger</h2>
    <panel>  
        <form class="create-form">
          <div class="form-group">
            <label for="newburger">Order</label>
            <input type="text" id="newburger" name="burger">
            <button type="submit" class="btn btn-success" id="addburger">
                <span class="fa fa-cutlery"></span>
           </button>
          </div>
        </form>
    </panel>
  </div> --}}
</div>
<div class="row">
  <div class= "col-sm-6">
    <div class="panel panel-default">
      <div class="panel-heading" id="rightside"><strong>Order the Burger</strong></div>
      <div class="panel-body">
        <ul>
          {{#each burgers}}
          {{#unless devoured}}
          {{> burger-block devoured=false}}
          {{/unless}}
          {{/each}}
          </ul>

          <h2>Burger Devoured!</h2>

        <ul>
          {{#each burgers}}
          {{#if devoured}}
          {{> burger-block devoured=true}}
          {{/if}}
          {{/each}}
          </ul>



      </div>
    </div>
  </div>
</div>





{{!-- <h1>Cats!</h1>

<h2>Cats that are not sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block ordered=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block ordered=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="ordered">Ordered</label><br>
    <input type="radio" name="ordered" value="1" checked> ordered<br>
    <input type="radio" name="ordered" value="0"> devoured
  </div>

  <button type="submit">Add Burger</button>
</form> --}}